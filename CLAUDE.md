# Project overview
- Yarn 4(Berry)와 TurboRepo를 사용하는 TypeScript 모노레포다. 루트 `package.json`의 `workspaces`와 `turbo.json`으로 워크스페이스를 관리한다.
- `apps/api`는 NestJS(Express) 백엔드 애플리케이션이다. 백엔드 전용 규칙은 `apps/api/CLAUDE.md`를 따른다.
- `apps/web`는 Vite + React + TypeScript 프론트엔드 애플리케이션이다. 프론트엔드 전용 규칙은 `apps/web/CLAUDE.md`를 따른다.
- `apps/admin`은 Next.js(App Router) + shadcn/ui 기반 관리자 전용 프론트엔드 애플리케이션이다. `apps/web`과 별개의 워크스페이스이며, 전용 규칙은 `apps/admin/CLAUDE.md`를 따른다.
- 공유 모듈이 필요해지면 `packages/` 아래에 워크스페이스로 추가한다.
- 패키지 매니저는 npm이 아니라 Yarn을 사용한다. Yarn 버전은 루트 `package.json`의 `packageManager` 필드를 source of truth로 삼고 Corepack으로 활성화한다.
- `nodeLinker`는 `.yarnrc.yml`에서 `node-modules`로 설정되어 있다 (PnP 미사용).
- `.claude/rules/`에 명시된 프로젝트 규칙을 준수해라.
- main, master, develop 브랜치에 직접 커밋하지 마라.
- 코드를 변경할 때는 작업별 브랜치를 생성하고 변경 사항을 테스트한 뒤, Conventional Commits(관습적 커밋) 규칙에 따라 커밋하고 해당 브랜치를 푸시해라.

# Working rules
- 설명과 질문, 작업 결과는 한국어로 작성한다.
- 타입 오류를 `any`나 타입 단언으로 회피하지 않는다.

### 1. 코딩하기 전에 생각하기

**추측하지 않는다. 혼란을 숨기지 않는다. 선택지와 트레이드오프를 명확하게 드러낸다.**

구현을 시작하기 전에:

- 전제나 가정이 있다면 명확히 밝힌다. 확실하지 않은 경우 질문한다.
- 여러 해석이 가능한 경우 임의로 하나를 선택하지 말고 가능한 해석을 제시한다.
- 더 단순한 방법이 있다면 먼저 제안한다. 요청한 방식이 불필요하게 복잡하거나 적절하지 않다면 그 이유를 설명한다.
- 요구사항이 불명확하다면 바로 구현하지 않는다. 어떤 부분이 불명확한지 구체적으로 설명하고 필요한 정보를 요청한다.

### 2. 단순함을 우선하기

**문제를 해결하는 데 필요한 최소한의 코드만 작성한다. 미래를 가정한 코드는 작성하지 않는다.**

- 요청하지 않은 기능은 추가하지 않는다.
- 한 번만 사용되는 코드를 위해 불필요한 추상화를 만들지 않는다.
- 요청하지 않은 확장성, 유연성, 설정 기능을 추가하지 않는다.
- 실제로 발생할 수 없는 상황을 위한 불필요한 예외 처리를 추가하지 않는다.
- 50줄이면 해결할 수 있는 문제를 200줄로 구현했다면 다시 단순화한다.

스스로 다음을 확인한다.

> "시니어 엔지니어가 이 구현을 보고 불필요하게 복잡하다고 생각할까?"

그렇다면 더 단순하게 만든다.

### 3. 필요한 부분만 정확하게 수정하기

**반드시 필요한 코드만 수정한다. 내가 만든 변경으로 발생한 문제만 정리한다.**

기존 코드를 수정할 때:

- 요청과 관계없는 주변 코드, 주석, 포맷을 임의로 개선하지 않는다.
- 문제가 없는 코드를 불필요하게 리팩터링하지 않는다.
- 내가 선호하는 방식과 다르더라도 기존 프로젝트의 코드 스타일과 패턴을 따른다.
- 관련 없는 미사용 코드나 오래된 코드를 발견하더라도 임의로 삭제하지 않는다. 필요하다면 사용자에게 알려준다.

내 변경으로 인해 더 이상 사용되지 않는 코드가 생긴 경우:

- 내가 수정하면서 사용되지 않게 된 `import`, 변수, 함수 등은 제거한다.
- 기존부터 존재하던 미사용 코드는 별도의 요청이 없는 한 제거하지 않는다.

판단 기준:

> 변경된 모든 라인은 사용자의 요청과 직접적인 연관성을 설명할 수 있어야 한다.

### 4. 목표 중심으로 작업하기

**완료 조건을 먼저 정의하고, 검증될 때까지 반복한다.**

작업 요청을 검증 가능한 목표로 변환한다.

예:

- `"validation 추가"`
  → `"잘못된 입력에 대한 테스트를 작성하고, 해당 테스트가 통과하도록 구현한다."`

- `"버그 수정"`
  → `"버그를 재현하는 테스트를 먼저 작성하고, 수정 후 테스트가 통과하도록 한다."`

- `"X 리팩터링"`
  → `"리팩터링 전후에 기존 테스트가 모두 통과하는지 확인한다."`

여러 단계가 필요한 작업이라면 구현 전에 간단한 계획을 작성한다.

```text
1. [작업] → 검증: [확인 방법]
2. [작업] → 검증: [확인 방법]
3. [작업] → 검증: [확인 방법]
```

명확한 완료 조건이 있다면 해당 조건을 만족할 때까지 스스로 구현과 검증을 반복한다.

완료 기준이 불명확한 경우 임의로 범위를 확대하지 말고 필요한 조건을 확인한다.

---

### 이 지침이 제대로 적용되고 있는지 판단하는 기준

- 요청과 관계없는 변경 사항(diff)이 줄어든다.
- 과도한 설계나 추상화로 인해 코드를 다시 작성하는 일이 줄어든다.
- 잘못 구현한 뒤 수정하기보다 필요한 확인이 구현 전에 이루어진다.

# Commands
- 의존성 설치: `yarn install`
- 전체 테스트: `yarn test`
- 변경 파일 확인: `git diff --stat && git diff`
- 앱별 빌드/실행 명령은 해당 앱의 CLAUDE.md 참고 (`apps/api/CLAUDE.md`, `apps/web/CLAUDE.md`)

# Verification
코드 변경 후 다음을 수행한다.

1. 변경된 파일의 타입 오류를 확인한다.
2. 관련 테스트를 실행한다.
3. 영향 범위가 넓으면 해당 앱의 빌드를 실행한다 (세부 사항은 앱별 CLAUDE.md 참고).
4. 수정 파일, 검증 결과, 남아 있는 위험 요소를 정리한다.

# Prohibited actions
- `.env` 파일을 수정하지 않는다.
- 요청 없이 패키지를 추가하거나 삭제하지 않는다.
- 요청 없이 Git commit, push, reset을 실행하지 않는다.
- AWS 운영 리소스에 변경 명령을 실행하지 않는다.
- `apps/api/.env`, `apps/api/.env.local`에는 실제 자격 증명(AWS, DB, JWT 등)이 들어있다. 내용을 출력하거나 다른 곳에 복사하지 않는다.